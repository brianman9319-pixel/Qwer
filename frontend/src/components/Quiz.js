import React from 'react';

const Quiz = () => {
    return (
        <div>
            <h1>Quiz Component</h1>
            {/* Add quiz questions and logic here */}
        </div>
    );
};

export default Quiz;